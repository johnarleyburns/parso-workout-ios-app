import SwiftUI

// Placeholder — built out under FR-3.
struct TodayView: View {
    var body: some View {
        NavigationStack {
            Text("Today")
                .navigationTitle("Today")
        }
    }
}
