# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
*.xcscmblueprint
DerivedData/
build/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Generated by XcodeGen — the project is defined in project.yml, so the
# .xcodeproj is disposable. Remove this line if you create the project by hand.
#*.xcodeproj

# Swift Package Manager
.build/
.swiftpm/
# Package.resolved IS committed (pins dependency versions) — do not ignore.

# Secrets / local config (never commit signing or keys)
*.xcconfig.local
secrets.plist
