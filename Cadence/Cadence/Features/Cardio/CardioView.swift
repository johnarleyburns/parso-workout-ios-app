import SwiftUI

// Placeholder — built out under FR-2.
struct CardioView: View {
    var body: some View {
        NavigationStack {
            Text("Cardio")
                .navigationTitle("Cardio")
        }
    }
}
